# 屈氏宗谱 Caddy 反向代理配置
# 监听 9997 端口，代理到后端 Express(5001) 和 Nuxt(3000)

:9997 {
    # API、管理后台、上传文件 → Express
    reverse_proxy /api/* localhost:5001
    reverse_proxy /admin/* localhost:5001
    reverse_proxy /uploads/* localhost:5001

    # 其余请求 → Nuxt SSR
    reverse_proxy localhost:3000
}
